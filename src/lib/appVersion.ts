/**
 * User-facing app version shown in the UI.
 * Keep in sync with package.json when shipping visible changes.
 */
export const APP_VERSION = "0.3.1";

export const APP_VERSION_LABEL = `v${APP_VERSION}`;
